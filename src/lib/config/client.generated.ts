import type {
    DeepPartial,
    ClientConfig,
  } from "./client";
  
  /**
   * CONFIGURACIÓN GENERADA AUTOMÁTICAMENTE
   *
   * Este archivo será escrito por el instalador.
   *
   * En una instalación nueva puede permanecer vacío.
   * Solo es necesario guardar los valores que cambian
   * respecto de client.defaults.ts.
   *
   * IMPORTANTE:
   * No agregar reglas de negocio manualmente aquí.
   */
  export const generatedClientConfig:
    DeepPartial<ClientConfig> = {};
  